---
# clang-tidy configuration for RetDec decompiler project
# This enables static analysis for bug detection, modernization,
# readability improvements, and performance optimizations.

Checks: >
  bugprone-*,
  modernize-*,
  readability-*,
  performance-*,
  -modernize-use-trailing-return-type,
  -readability-identifier-length

CheckOptions:
  # Class naming convention: CamelCase
  - key: readability-identifier-naming.ClassCase
    value: CamelCase

  # Function naming convention: camelCase
  - key: readability-identifier-naming.FunctionCase
    value: camelCase

  # Variable naming convention: camelCase
  - key: readability-identifier-naming.VariableCase
    value: camelCase

  # Method naming convention: camelCase (consistent with functions)
  - key: readability-identifier-naming.MethodCase
    value: camelCase

  # Parameter naming convention: camelCase (consistent with variables)
  - key: readability-identifier-naming.ParameterCase
    value: camelCase

# Inherit parent configurations if they exist
InheritParentConfig: false

# Header filter regex - analyze headers in the project
HeaderFilterRegex: '.*'

# Use color in diagnostics
UseColor: true

# Format style for fix-its
FormatStyle: file
...
